#WAP to reverse a given array without using built in function
print("Task 14:reverse a given array without using built in function")
arr=[1,2,3,4,5,6,7]
def reverse_array(arr):
    start=arr[0]-1
    end=len(arr)-1

    while(start < end):
        arr[start],arr[end]=arr[end],arr[start]
        start += 1
        end -= 1

    return arr
print(reverse_array(arr))
